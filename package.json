{
  "name": "backtest-k",
  "version": "0.1.0",
  "private": true,
  "description": "한국 투자자용 포트폴리오 백테스트 정적 웹앱 (빌드 없음, ESM + 정적 JSON)",
  "type": "module",
  "scripts": {
    "dev": "python -m http.server 8123",
    "test": "node --test tests/*.test.mjs && python -m unittest discover -s tests"
  },
  "engines": {
    "node": ">=20"
  }
}
